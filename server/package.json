{
  "name": "peopleconnector-backend",
  "version": "1.0.0",
  "description": "Fake backend server to support client app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch ./db.json"
  },
  "author": "Ruga-X The Team",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.14.2"
  }
}
